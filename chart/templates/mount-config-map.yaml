apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configMap.name }}
  labels:
    app: {{ .Values.app.name }}
data:
  {{ .Values.configMap.file }}: |
    {{ .Files.Get (print "config/" .Values.configMap.file) | nindent 4 }}
