apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.template.name }}
  labels:
    app: {{ .Values.template.app }}
spec:
  type: NodePort
  ports:
    - port: {{ .Values.template.port }}
      targetPort: {{ .Values.template.port }}
      protocol: TCP
      name: {{ .Values.template.name }}
  selector:
    app: {{ .Values.template.app }}
